#import "./_TripBlock.gql"

query UserPageQuery($username: String!) {
  user(username: $username) {
    id
    name
    isViewer
    username
    trips {
      ...TripBlockFragment
      viewerCanEdit
    }
  }
}
